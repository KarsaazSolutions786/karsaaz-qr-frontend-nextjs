# Team dev — frontend Docker or npm, API on host PC
copy .env.team.example .env
# Then set HOST_IP (from team lead):
NEXT_PUBLIC_API_URL=http://HOST_IP:8000
NEXT_PUBLIC_APP_URL=http://localhost:3005
NODE_ENV=development
